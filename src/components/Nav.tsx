export const Nav = () => {
  return (
    <nav className=" fixed select-none font-athiti top-5 left-5 font-semibold text-2xl text-amber-500">
      <a href="#" onClick={(e) => e.preventDefault()}>
        ปีใหม่ตอนไหน
      </a>
    </nav>
  );
};
